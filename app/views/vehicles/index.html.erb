<div class="container">
  <h1>Show all <%= params[:category] %>s</h1>

  <% @vehicles.each do |vehicle| %>
    <div>
      <%= link_to vehicle_path(vehicle) do %>
      <%= vehicle.description %>
      <%= vehicle.price_day %>
      </div>
      <% end %>
  <% end %>
</div>
